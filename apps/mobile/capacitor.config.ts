import { CapacitorConfig } from '@capacitor/cli';

// Configure the capacitor.
export default <CapacitorConfig>{
  appId: 'com.yokize.organize',
  appName: '@organize/mobile',
  webDir: 'dist',
  bundledWebRuntime: false
};
